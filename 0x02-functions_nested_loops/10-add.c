/**
  * add - adds two integers and return the result
  * @a: this parameter accepts int
  * @b: this parameter accepts int
  * Return: returns an int
  */

int add(int a, int b)
{
	return ((a + b));
}
